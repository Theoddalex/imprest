# Core service
mcp>=1.28              # MCP server (FastMCP)
pydantic-settings>=2   # typed settings
pyyaml>=6              # policy.yaml

# Ethereum (testnet)
web3>=7
eth-account>=0.13

# Demo agent (examples/)
langchain>=1.3,<2
langchain-openai>=1.3,<2
langgraph>=1.2,<2
langchain-mcp-adapters>=0.3
python-dotenv>=1.0

# Dev
pytest>=8
